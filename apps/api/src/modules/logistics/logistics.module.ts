import { Module } from '@nestjs/common';

// Fase 2 (Etapa 10): cadastro de entregadores, matching de corridas em cascata,
// posição do entregador e ciclo de vida da entrega.
@Module({})
export class LogisticsModule {}
